import { notFound } from "next/navigation";

/** The old illustrative report is retired; the canonical Free result is /ru/check. */
export default function RussianSampleReportPage(): never {
  notFound();
}
